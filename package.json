{
  "scripts": {
    "docker:build": "docker compose build --pull --no-cache",
    "docker:build:backend": "docker compose build backend --pull --no-cache",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "backend:exec": "docker compose exec backend bash"
  }
}